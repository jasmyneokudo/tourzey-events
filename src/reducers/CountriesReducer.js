import data from './continents.json';

export default () => data;

// const selectCountryReducer =  (state = null, action) => {

//     switch(action.type){

//         case 'select_country':
//             return action.payload;

//         default:
//             return state;
//     }
    
// };

// export default selectCountryReducer;